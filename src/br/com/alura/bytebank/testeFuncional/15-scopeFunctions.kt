package br.com.alura.bytebank.testeFuncional

/*
Let: retorna o resultado das instruções/operações, faz a referencia com 'it'

Apply: retorna o próprio objeto com as modificações, faz a referencia com 'this'

Run/With: retorna o resultado das instruções/operações, faz a referencia com 'this'

Also: retorna o próprio objeto com as modificações, faz a referencia com 'it'


run() sem extensão para executar qualquer instrução retornando ou não uma computação.
also() para imprimir o que está acontecendo no código e devolver o objeto de contexto.
apply() para inicializar o objeto de contexto e devolvê-lo.
with() para computar e devolver algo utilizando os membros do objeto de contexto.

 */